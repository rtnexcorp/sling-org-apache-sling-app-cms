{
  "name": "sling-cms-it",
  "version": "1.0.0",
  "description": "Integration tests for Apache Sling CMS",
  "main": "tests/index.js",
  "scripts": {
    "cypress:install": "./node_modules/.bin/cypress install --force",
    "cypress": "./node_modules/.bin/cypress open",
    "cypress:open": "./node_modules/.bin/cypress open",
    "test": "./node_modules/.bin/cypress run",
    "test:standalone": "SLING_MODE=standalone npm run test",
    "test:author": "SLING_MODE=author npm run test",
    "test:renderer": "SLING_MODE=renderer npm run test",
    "test:spec": "./node_modules/.bin/cypress run --spec",
    "wait-for-ready": "node cypress/ready.js",
    "ci": "npm run cypress:install && npm run wait-for-ready && npm run test",
    "ci:author": "npm run cypress:install && npm run wait-for-ready && npm run test:author"
  },
  "author": "Apache Software Foundation",
  "license": "Apache-2.0",
  "devDependencies": {
    "chai": "^4.5.0"
  },
  "dependencies": {
    "@cypress-audit/pa11y": "^1.4.2",
    "cypress": "^15.7.1",
    "uuid": "^11.1.0"
  },
  "overrides": {
    "semver": "^7.7.3",
    "glob": "^11.0.3",
    "rimraf": "^6.0.1",
    "puppeteer": "^24.15.0"
  }
}
